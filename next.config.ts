import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Intentionally minimal — nothing here needs custom config yet.
  // Common additions later: images.remotePatterns if you add author avatars,
  // or redirects()/headers() for custom security headers.
}

export default nextConfig
